# ─── product-service.properties ───────────────────────────────────────────────
# These properties are served ONLY to product-service (matched by spring.application.name)
# They OVERRIDE values from application.properties (the shared file above)

# App metadata for /actuator/info
info.app.name=product-service
info.app.version=1.0.0
info.app.description=REST API for managing products in the e-commerce platform
info.app.author=Ankur Rana

# Feature: max products before health indicator reports DOWN
app.product.store.max-capacity=1000
